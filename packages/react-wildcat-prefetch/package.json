{
  "name": "react-wildcat-prefetch",
  "version": "1.0.0-beta2",
  "description": "A React decorator to prefetch component data.",
  "main": "dist/index.js",
  "dependencies": {
    "exenv": "^1.2.0",
    "hoist-non-react-statics": "^1.0.3",
    "invariant": "^2.1.2"
  },
  "scripts": {
    "build": "npm run clean && npm run compile",
    "build-test": "npm run build && npm run compile-test",
    "clean": "rimraf dist",
    "compile": "babel src --out-dir dist",
    "compile-test": "babel test --out-dir dist",
    "prepublish": "env NODE_ENV=production npm run build",
    "pretest": "npm run build-test"
  },
  "author": "engineers@nfl.com",
  "license": "MIT"
}
